-- ⚡ Tezz v0.2.0

const version = "0.2.0"

service HelloAPI on 3000 {

  route GET "/" {
    let name = request.query.name || "World"
    print("Greeting: " + name)
    
    respond 200 {
      message: "Hello, {name}! ⚡",
      version: version,
      status: true
    }
  }

}
